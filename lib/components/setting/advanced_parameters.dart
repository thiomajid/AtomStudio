import 'package:flutter/material.dart';

class AdvancedParameters extends StatelessWidget {
  const AdvancedParameters({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
