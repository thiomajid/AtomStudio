import 'package:flutter/material.dart';

class VideoParameters extends StatelessWidget {
  const VideoParameters({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
